# 中医四部经典研读 Skill — Coding Plan

> 版本：0.6（功能验收与候选包已完成）  
> 产品暂名：中医四部经典研读  
> Skill 名：`tcm-classics-study`  
> 目标平台：WorkBuddy / 兼容 Agent Skills 的运行环境

## 1. 已确认的产品决策

1. 一次性建设四个经典体系、六部独立文本，不采用“先两部、再扩充”的产品路线：
   - 黄帝内经体系：《素问》《灵枢》
   - 伤寒杂病论体系：《伤寒论》《金匮要略》
   - 《难经》
   - 《温病条辨》
2. 《伤寒论》《金匮要略》分别建库、分别引用；产品中可以归组，但不拼造成一部“恢复版《伤寒杂病论》”。
3. 原典库与历代医家扩展库物理或逻辑分层；默认可以同时检索，但回答必须明确区分二者。
4. 首版同步准备六部文本对应的医家扩展语料，不等待后续版本。
5. 现代医学不是偶尔附加的装饰层：只要问题存在合理、可核验的现代医学对应，就默认输出；纯文字定位、版本考据等没有合理对应的问题可省略。
6. 断网不阻塞原典和医家层；如有最近一次已核验的现代资料缓存，可以标注核验日期后使用，否则明确说明未能联网核验。
7. 面向60岁以上用户优化可读性：固定层级、固定颜色、较大字号、高对比度；不能只依靠颜色表达含义。
8. 产品定位为经典研读与科学对照，不提供个体诊断、辨证处方、剂量换算或停换药建议。
9. 《温热论》《湿热病篇》作为明确标记的“学术源流”纳入首版，但绝不称为《温病条辨》的后世注家。
10. Wikisource 文本缺失或质量不合格时，允许采用 Kanripo 等许可明确的公开古籍库；必须记录来源、底本和许可。
11. WorkBuddy 可稳定提供网页搜索和网页读取能力；现代医学层优先复用该能力，并以结构化医学接口作补充。
12. 用户界面只显示简体阅读文本，不提供“展开繁体底本”功能；构建系统仍保留繁体来源快照用于校核、追溯和重新转换。
13. 先完成可运行产品主线，再集中精修并行文本质量线；隔离内容不得进入检索或引用，当前工作底本风险必须如实披露，但不再阻塞功能开发和候选版实机验收。

## 2. 产品目标与非目标

### 2.1 目标

- 能按自然语言查找六部经典中的原文并给出稳定、可回查的出处。
- 将“原典原文”“AI研读解释”“后世医家原文与解释”“现代医学证据”清晰分层。
- 支持原文定位、逐段研读、医家比较、跨经典比较、现代医学对照。
- 经典问题默认使用本地离线检索，避免全量加载、全表扫描和不必要联网。
- 对现代医学证据说明研究对象、证据类型、直接性、局限和更新时间，不以“有论文”代替“有效证据”。
- 保留完整来源、版本、修订号、许可信息和繁简转换链路。

### 2.2 非目标

- 不恢复或重构已经失传的《伤寒杂病论》原貌。
- 不把现代疾病与中医证候做一对一等同。
- 不把动物实验、细胞实验或单篇低质量研究表述为临床疗效确证。
- 不追求无限收录历代医家；首版以高关联、高认可、可合法取得且文本质量可控为准。
- 不把 Skill 做成问诊或自动开方产品。

## 3. 语料范围

### 3.1 原典库

| 体系 | 独立文本 | 内部代码建议 | 切分主结构 |
|---|---|---:|---|
| 黄帝内经 | 《素问》 | `SW` | 卷、篇、问答组合 |
| 黄帝内经 | 《灵枢》 | `LS` | 卷、篇、问答组合 |
| 伤寒杂病论 | 《伤寒论》 | `SHL` | 卷、篇、条文、方 |
| 伤寒杂病论 | 《金匮要略》 | `JKY` | 篇、条文、方 |
| 难经 | 《难经》 | `NJ` | 第一难至第八十一难及内部问答 |
| 温病 | 《温病条辨》 | `WBTB` | 卷、上中下焦、篇、条文、作者自注 |

每部书在正式导入前必须形成“来源审批表”，至少包括页面标题、底本说明、Wikisource 修订号、完整性、正文/注文边界、许可、抓取日期和 SHA-256。

### 3.2 医家扩展库首选清单

选择原则：与对应经典关系直接、学术史认可度高、观点具有代表性、与其他入选者有互补性、公开文本质量可控。以下为首选方案；正式锁定前仍需做版本与完整性审计。

#### A. 《素问》扩展

1. 王冰注、林亿等校正《重广补注黄帝内经素问》
   - 类型：基础性旧注、校正
   - 注意：必须区分经文、王冰注、“新校正云”等不同说话者。
2. 张志聪《黄帝内经素问集注》
   - 类型：逐篇注释、理论阐发
   - Phase 0 调整：替换未找到合格公开全文的马莳《黄帝内经素问注证发微》
3. 张介宾《类经》
   - 类型：重分类、综合阐释；同时关联《素问》《灵枢》

备选：高士宗《黄帝素问直解》、丹波元简《素问识》；马莳本在找到许可和质量合格的公开文本后可重新评估。

#### B. 《灵枢》扩展

1. 张志聪《黄帝内经灵枢集注》
   - 类型：集注、理论阐释
2. 张介宾《类经》
   - 类型：重分类、综合阐释；与《素问》共用一个作品库，按段落映射
3. 丹波元简《灵枢识》
   - 类型：汇校、辨析、分歧
   - Phase 0 调整：替换未找到合格公开全文的马莳《黄帝内经灵枢注证发微》

备选：杨上善《黄帝内经太素》中可明确对应的材料；马莳本在找到许可和质量合格的公开文本后可重新评估。

#### C. 《伤寒论》扩展

1. 成无己《注解伤寒论》——现存早期系统注释
2. 方有执《伤寒论条辨》——重订、条辨、错简学说
3. 喻昌《尚论篇》——重订与理论发挥
4. 柯琴《伤寒来苏集·伤寒论注》——六经、方证及编次阐释
5. 尤怡（尤在泾）《伤寒贯珠集》——治法分类与综合阐释

要求：回答时必须把“宋本原文”“注家引述的仲景文”“注家自己的判断”分开。

#### D. 《金匮要略》扩展

1. 徐彬《金匮要略论注》——系统注释
2. 尤怡《金匮要略心典》——代表性注释与临床阐发
3. 陈修园《金匮要略浅注》——普及性强、便于初学研读

Phase 0 已排除程林《金匮要略直解》：Wikisource 自动搜索命中的《金匮玉函经二注》实为周扬俊著，不能冒名替代；其余三家已满足范围。备选为魏荔彤《金匮要略方论本义》等，正式入库取决于公开版本质量。

#### E. 《难经》扩展

1. 滑寿《难经本义》——代表性系统注释
2. 徐大椿《难经经释》——经义辨析与考释
3. 王九思辑《难经集注》——汇集吕广、杨玄操等旧注

要求：《难经集注》内部尽可能保留每条注文的实际署名，不能统一写成王九思个人观点。

#### F. 《温病条辨》扩展

严格晚于吴瑭、且有较高关联度的首选：

1. 王士雄（王孟英）《温热经纬》——温病学术汇编与发展
2. 雷丰（雷少逸）《时病论》——温病、时病临床理论发展

叶霖《增订温病条辨》因 Phase 0 未找到许可和质量均可审计的公开全文而延期，不阻塞首版。

另设“学术源流”而非“后世注家”类别：

- 叶桂（叶天士）《温热论》
- 薛雪《湿热病篇》

二者早于《温病条辨》或属于其重要前导学术来源，关联度很高，首版予以纳入，但必须标记为 `lineage_predecessor`，不得标成吴瑭之后的注家。

### 3.3 扩展语料的关系标签

每条扩展材料必须使用一个或多个明确标签：

- `direct_commentary`：逐条或直接注释
- `textual_collation`：校勘、版本辨析
- `reordering`：重订、重新编次
- `classification`：重新分类整理
- `theoretical_extension`：理论发挥
- `clinical_extension`：临床发挥
- `disagreement`：明确分歧
- `lineage_predecessor`：前导学术源流，不是后世注家

## 4. 文本与来源治理

### 4.1 三种文本字段

每个可引用段落至少保留：

- `text_traditional`：固定版本的繁体工作底本
- `text_simplified`：面向用户显示的简体阅读本
- `text_search`：去标点、异体字映射和检索规范化后的内容，只用于索引

用户界面的直接引文只取 `text_simplified`，禁止引用 `text_search`。`text_traditional` 不向用户展开，仅供来源校核、繁简转换审计和后续重建使用。

### 4.2 来源优先级

1. 优先使用中文维基文库中底本、修订号、完整性明确的文本。
2. Wikisource 缺失或质量不合格时，允许采用 Kanripo、Chinese Text Project 或其他许可明确的开放古籍库。
3. 任何替代来源必须先核验许可、底本和完整性，并在 `source-manifest.json` 中单独记录；来源审计报告应明确说明替代理由。
4. 不因“网上可看”就默认允许打包分发。

### 4.3 构建期繁简转换

- OpenCC 只作为构建工具，不作为每次查询的运行时依赖。
- 保留转换前文本、转换后文本、OpenCC 版本和自定义例外词典。
- 对中医术语、药名、方名、古今字建立回归测试。
- 自动检查 `�`、乱码模式、罕见字符丢失、HTML/模板残留和异常空白。

### 4.4 结构化切分原则

- 优先按书本结构、问答、条文和方剂边界切分，不按固定字数机械切割。
- 方名、主治上下文、组成、煎服法不得被无意义拆散。
- 超长注文允许二级切分，但保留父段落ID和前后关系。
- 每段具有稳定ID、父级篇章、前后段ID、来源ID和内容哈希。

## 5. 检索架构

### 5.1 存储

用预构建的 SQLite 数据库替代运行时加载大 JSON：

```text
data/classics.sqlite       原典、医家文本、元数据、FTS/倒排索引
data/evidence-cache.sqlite 已核验现代医学资料缓存（可选、可更新）
```

建议核心表：

- `works`
- `sources`
- `passages`
- `passage_links`
- `commentary_relations`
- `aliases`
- `fts_passages` 或自建 n-gram postings
- `evidence_topics`
- `evidence_sources`
- `evidence_cache`

### 5.2 中文检索

- 预生成一元、二元、三元或二至四元字符词项；具体组合通过体积和召回测试确定。
- 支持简繁、异体字、方名别名、人名字号和现代常用表述。
- 精确短语、篇名、方名、标题命中加权。
- 查询规范化在每次请求中只做一次。
- 候选集使用索引取得，不扫描全部段落。
- 只对有限候选做二次排序，并使用去重/MMR避免连续返回多个高度重复段落。

### 5.3 查询路由

路由字段：

- `canon`：`huangdi` / `zhongjing` / `nanjing` / `wenbing`
- `work`：六部独立文本
- `layer`：`core` / `commentary`
- `author` / `relation_type`

默认流程：

1. 从用户问题推断可能经典和1—3个经典检索词。
2. 一次调用本地搜索，同时返回原典候选和对应医家候选。
3. 明确要求跨经典比较时扩大范围；低置信度时进行一次全库回退。
4. 默认每层返回2—3条、总计不超过约2500个汉字；需要时按ID展开上下文。

### 5.4 运行时命令草案

```bash
python scripts/search.py search \
  --query "用户原问题" \
  --terms "治未病,未病先防" \
  --canon auto \
  --layers core,commentary \
  --limit-core 3 \
  --limit-commentary 3 \
  --format json

python scripts/search.py passage --id SW-...
python scripts/search.py context --id SW-... --before 1 --after 1
python scripts/search.py toc --work suwen
```

### 5.5 不默认使用向量模型

首版使用“路由 + 字符索引 + 术语表 + 精确短语加权”。只有在验收集证明白话改写召回明显不足时，再加入可选语义召回；不得为了名义上的 RAG 引入大模型冷启动和网络依赖。

## 6. 现代医学层

### 6.1 是否触发现代层

将问题分为四级：

- `M0 纯文献`：原句、篇章、版本、某注家如何解释；通常不输出现代层。
- `M1 概念对照`：病因、病机、脏腑、经络、证候等；输出简短现代概念边界，重点说明能否对应及不能如何对应。
- `M2 证据/安全`：症状、疾病、方剂、药物、疗效、不良反应、检查、预后；默认输出完整现代层。
- `M3 急症风险`：先输出纯文本紧急分流，再提供必要的现代安全信息；古方不能作为替代。

判断偏向科学对照：只要存在合理现代研究对象，就进入 `M1` 或 `M2`；但不为纯字词考据硬凑现代解释。

### 6.2 现代证据获取

采用“本地登记与缓存优先，联网更新补充”的混合方案：

1. 先查 `evidence-cache.sqlite` 中已经人工或程序核验的主题资料。
2. 缓存没有覆盖、已经过期或问题要求最新进展时联网。
3. 在线检索优先级：
   - 卫生主管部门、药监机构、WHO、CDC、NICE 等官方来源
   - 正式临床指南及专业学会共识
   - Cochrane、系统综述、Meta分析
   - PubMed 可核验的随机对照试验
   - 观察性研究
   - 机制、动物和细胞研究（只能作为低直接性背景）
4. 商业宣传页、无作者日期网页、单一新闻报道不得作为疗效依据。
5. 联网失败时，原典和医家层立即继续；有缓存则标注“最近核验日期”，无缓存则说明未提供现代结论。

联网适配器优先复用 WorkBuddy 已稳定提供的网页搜索和网页读取能力，同时提供 PubMed E-utilities 等无密钥接口作为结构化补充。实施期仍需验证具体工具调用格式、超时和返回结构。所有外部页面按不可信输入处理。

### 6.3 现代层输出规则

每条现代结论必须包括：

- 现代医学结论
- 与古典概念的直接性：直接研究 / 间接相关 / 不可直接对应
- 证据类型
- 证据可信度：较高 / 中等 / 有限 / 很有限 / 未见直接临床证据
- 主要限制
- 机构或作者、标题、日期、链接、DOI/PMID（如有）
- 在线核验日期

“证据可信度”是产品内的清晰提示；除非原来源正式采用 GRADE，不冒充正式 GRADE 分级。

必须主动呈现阴性、不一致或低质量结果，禁止只挑支持中医疗效的论文。

## 7. 回答协议与可视化

### 7.1 固定三层

#### 🟩 A｜原典

- 原典原文（逐字来自数据库）
- AI研读解释（明确标为解释）
- 书名、卷篇、段落ID、工作底本和来源

#### 🟧 B｜历代医家

- 医家原文
- 医家、作品、卷篇、段落ID
- 关系标签：注释/校勘/重订/理论发挥/分歧/学术源流
- AI解说：说明它和原典是解释、补充、改写还是冲突

#### 🟦 C｜现代医学

- 现代结论
- 证据类型与可信度文字标签
- 与古典概念的对应限制
- 参考来源与核验日期

即使颜色无法显示，也必须依靠 `A/B/C`、标题和图标保持结构，因此不能只用绿/橙/蓝表达语义。

### 7.2 老年用户可读性

- 卡片标题20px左右，层标题18px，正文至少16px，来源至少14px。
- 行高1.7—1.8，浅底深字，高对比度。
- 每次先给“一句话结论”，再给三层材料。
- 原文、解释和来源分别换行，不混排成长段。
- 默认每层只展示最有代表性的2—3条，提供“展开上下文/查看更多医家”。
- 避免宽表格、密集脚注、纯灰小字和红绿二色作为唯一标识。
- 重要安全提醒必须在卡片外先用纯文本显示。

### 7.3 两种渲染实现

1. `accessible-cards`：优先方案，使用一个轻量的固定卡片模板，不使用渐变、阴影、动画或外部资源。
2. `accessible-markdown`：降级方案，使用彩色方块图标、二级标题、引用块、分隔线和加粗，无HTML依赖。

开发时制作同一答案的两种原型，由实际用户试用后确定默认方式。不要把大段CSS规范放进每次必读的 `SKILL.md`；样式规则应压缩为固定模板或独立按需参考。

## 8. Skill 运行流程

1. 识别请求模式：只看原文 / 原文研读 / 医家比较 / 跨经典比较。
2. 检测现实健康和急症风险；`M3` 先安全分流。
3. 生成经典范围和1—3个检索词。
4. 一次本地调用检索原典及相应扩展材料。
5. 所有直接引文从检索结果复制，不凭模型记忆补引。
6. 判定 `M0—M3`；`M1+` 获取或更新现代医学证据。
7. 按 A/B/C 固定层级渲染。
8. 检索未命中时使用“当前所收版本未检得”，不能推导为历史上绝对不存在。

## 9. 项目结构建议

```text
tcm-classics-study-project/
├── skill/                         # 最终打包内容
│   └── tcm-classics-study/
│       ├── SKILL.md
│       ├── scripts/
│       │   ├── search.py
│       │   ├── modern_evidence.py
│       │   ├── study.py
│       │   ├── render.py
│       │   └── self_check.py
│       ├── data/
│       │   ├── classics.sqlite
│       │   ├── evidence-cache.sqlite
│       │   └── source-manifest.json
│       ├── references/
│       │   ├── answer-policy.md
│       │   ├── modern-evidence-policy.md
│       │   ├── safety.md
│       │   └── source-notes.md
│       └── NOTICE.md
├── builder/                       # 不进入普通用户运行包
│   ├── fetch_wikisource.py
│   ├── extract.py
│   ├── convert_simplified.py
│   ├── segment.py
│   ├── build_index.py
│   ├── validate_corpus.py
│   └── build_release.py
├── sources/
│   ├── source-lock.candidate.json # 质量闸门未通过时的固定修订候选
│   ├── source-lock.json           # 仅质量闸门通过后生成
│   ├── source-manifest.json
│   ├── raw/                       # 原始繁体快照
│   └── processed/
├── schemas/
├── tests/
│   ├── golden_questions.jsonl
│   ├── corpus_checks/
│   ├── retrieval/
│   ├── safety/
│   └── rendering/
├── docs/
│   ├── corpus-report.md
│   ├── benchmark.md
│   └── release-checklist.md
└── README.md
```

## 10. 实施阶段

### Phase 0：环境与来源审计（已完成）

- 验证 WorkBuddy 的 Python、SQLite、FTS5 和可视化能力，并确认已具备的网页搜索/网页读取工具的具体调用格式、超时和返回结构。
- 解包并审计现有 Skill，可复用文本必须重新通过来源和乱码检查。
- 为六部原典和全部首选扩展作品建立来源审批表。
- 对缺失、OCR质量差或许可不明确的候选，按已确认的公开来源准入规则选择替代文本并记录理由。

交付：环境报告、来源清单、最终医家入选表。

### Phase 1：完整语料构建（构建完成；发布质量闸门待消项）

已完成：

- 同阶段固定26种作品、973个页面修订；938页进入语料，不打包扫描影像。
- 保存 wikitext 快照、页面级与作品级 SHA-256，冻结 `SKchar`/`SKchar2` 依赖。
- 生成 `text_source`、`text_traditional`、`text_simplified`、`text_search`。
- 解析经文、注文、新校正、校语、问答和学术源流；不能可靠区分者保守标记为 `mixed`。
- 生成54,088条可追溯段落，其中53,798条可引用、290条进入隔离库，另丢弃3条纯标点格式碎片；完成结构、哈希、路径、ID、上下文链接、模板、HTML、PUA及繁简检查。
- 生成完整性清单、质量报告、逐页署名清单、NOTICE及候选来源锁。

当前质量闸门：

- 原始哈希 `pass`，结构完整性 `pass`，可引用语料未解字检查 `pass`，清洗 `pass-with-quarantine`，发布闸门 `blocked`。
- 原始快照不删字；35个显式缺字、342个 `HT/KT` 旧转录占位符和1处界面文字混入均保留在290个隔离段中，不参与搜索或回答。
- 五种核心底本需第二许可明确底本校核；《温病条辨》仍有2个核心隔离单元；王冰本有805页低校对等级。
- 因此当前只有 `source-lock.candidate.json`；构建器明确禁止在闸门未通过时生成最终 `source-lock.json`。

待完成后才算 Phase 1 发布验收通过：逐条修复可核验乱码/缺字、完成核心底本差异检查和抽检、处理王冰本低校对风险，并重跑全部测试。详情见 `tcm-classics-study-project/docs/phase1-summary.md`。

交付现状：可复现语料、候选来源锁、质量报告；最终 `source-lock.json` 待质量消项。

### Phase 2：索引与检索（候选原型已完成；发布仍受 Phase 1 闸门约束）

已完成：

- 建立规范化 SQLite schema 和预分词重叠二元字符 FTS5 索引；运行时不加载全量 JSON。
- 候选 `build/classics.sqlite` 约49.9 MiB，只含53,798条可引用段落，290条隔离段明确不入库。
- 实现经典/体系/医家别名路由、作品与层级过滤、精确词组加权、近重复抑制、段落回查、连续上下文和目录读取。
- 一次请求可同时返回原典及相关医家材料；CLI 只输出 `text_simplified` 作为引文字段。
- 当前数据库重跑12类查询各3次的开发机基准：内部检索中位数约4.1ms、P95约15.2ms；含 Python 进程启动端到端中位数约714ms、P95约1.21s；路由/召回检查24/24通过。精确数值和数据库SHA-256见 Phase 2 报告。
- 与旧 JSON 全扫描约1.29—1.38秒的中位耗时相比，索引内部不再受全库扫描开销支配；目标 WorkBuddy 电脑仍需实机验收。

交付现状：`build/classics.sqlite`、`skill/tcm-classics-study/scripts/search.py`、`docs/phase2-index-report.md`。该数据库标记为 `candidate`，不得绕过 Phase 1 最终来源锁发布。

### Phase 3：现代医学证据层（首个可运行版已完成）

已完成：

- 实现 `M0—M3` 分类、学术语境抑制误报、急症优先分流和“最新证据”强制刷新规则。
- 建立11个可信来源机构的登记与用途边界、25个现代主题、证据缓存schema和记录JSON Schema。
- 建立6条完整页面核验的首批种子记录，覆盖中药总体证据及产品安全、针灸疼痛证据及操作安全、卒中和严重过敏急症。
- 实现缓存有效期、过期禁引、来源域名检查、去身份化联网检索计划、搜索摘要禁引及网页提示注入防护。
- 实现断网、缓存缺失、缓存过期和来源无法核验时的降级；M3分流不依赖网络。
- 完成工具无关的 WorkBuddy 网页搜索/读取协议；目标电脑上的具体工具调用签名留待实机适配。
- 将经典数据库和现代证据缓存装入 `skill/tcm-classics-study/`，加入候选运行时清单和自检工具。

交付现状：`modern_evidence.py`、`evidence-cache.sqlite`、证据协议、来源/主题/记录清单、网页接入协议及 `docs/phase3-modern-evidence-report.md`。

### Phase 4：回答协议与交互原型（已完成代码与自动验证）

已完成：

- 精简 `SKILL.md`，并将 `study.py` 设为首选单调用入口；按需规则保留在 references。
- 实现 A/B/C 固定层级和不超过2500字的默认经典上下文预算；M3默认暂缓古籍检索。
- 实现可访问Markdown和自包含HTML两种渲染；HTML正文18px、来源16px、行高1.75，无外部资源、动画、渐变或阴影。
- 将研究包、Markdown渲染和M3分流冒烟纳入运行时自检；Phase 4的5项独立测试已纳入38项全量回归。

交付现状：两个可试用渲染版本及 `docs/phase4-interaction-report.md`。WorkBuddy实际HTML能力和具体用户设备反馈由使用方验证，不作为代码交付阻塞项。

### Phase 5：验收与候选发布（候选包已完成）

已完成：

- 建立150道固定验收题，覆盖六部原典、医家检索、M0—M3和安全边界；280/280项检查通过。
- 当前38项自动测试全部通过，Python编译检查、运行时自检和当前索引benchmark均通过。
- 构建 `dist/tcm-classics-study-candidate.zip`，并在全新临时目录解压后运行自检；外部SHA-256清单已生成。
- 提供 `docs/INSTALL.md`，说明安装、权限、断网、升级、回滚、自检和候选底本风险。

使用方后续事项（不属于代码完成条件）：

- 在目标 WorkBuddy 中导入ZIP，并按宿主实际接口配置网页搜索/读取工具；
- 按具体设备和用户偏好选择Markdown或HTML并调整显示设置。

交付现状：可运行候选 Skill ZIP、构建源码、来源清单、测试集、性能报告和安装说明均已完成。代码任务已完成；最终无条件发布仍受 Phase 1 文本质量闸门约束。

## 11. 验收指标

### 11.1 检索

- 本地检索 P95 目标小于200ms（最终以 WorkBuddy 实机为准）。
- 精确原句/方名/篇名问题 Recall@5 不低于95%。
- 白话改写问题 Recall@5 初始目标不低于85%，根据父亲真实问题集继续提高。
- 默认每层不超过3条，总检索上下文原则上不超过2500个汉字。
- 直接引文全部能按段落ID回查，禁止出现检索结果之外的伪引文。

### 11.2 语料

- 六部原典全部通过篇章完整性清单。
- 所有来源有版本、修订号、链接、日期和哈希。
- 所有扩展段落有医家、作品和关系类型。
- 自动检测不到替换字符和已知乱码模式。
- 繁简转换高频术语回归测试全部通过。

### 11.3 现代医学

- `M1+` 问题在联网正常时默认包含现代层。
- 所有现代结论至少有一个可验证来源；疗效结论优先要求指南或系统证据。
- 来源标题、机构/作者、日期、链接与 PMID/DOI（如有）可核验。
- 明确区分直接临床研究、间接研究和机制研究。
- 阴性或不一致证据不能被省略。

### 11.4 可用性

- 不看颜色也能识别 A原典/B医家/C现代。
- 正文字号不小于16px，来源不小于14px。
- 用户能在数秒内找到原文、解释和出处。
- 长答案先显示一句话结论和每层要点，不形成无层次的大段正文。

## 12. 风险与应对

| 风险 | 应对 |
|---|---|
| Wikisource 某些注本不完整或OCR质量差 | 来源审计；不达标即换底本或替换候选，不强行入库 |
| 注本混有经文、注文、校语 | 建立说话者字段和解析规则；抽样人工校验 |
| 扩展作品数量远大于六部原典 | 索引检索、按经典路由、每层限量，不默认读取全库 |
| 现代检索拖慢回答 | 缓存优先、超时降级；经典层不等待失败的现代检索 |
| 现代研究质量参差 | 来源优先级、证据直接性和局限字段；禁止论文数量投票 |
| 卡片生成增加上下文和输出 | 固定轻量模板；不重复生成大段CSS；提供Markdown降级 |
| 古今概念被错误等同 | 每条现代层设置“直接性/对应限制”字段 |
| 模型伪造原文或文献 | 引文仅来自工具结果；现代来源要求URL/PMID/DOI校验 |

## 13. 已确认事项与实施期验证

1. 《温热论》《湿热病篇》作为“学术源流”纳入首版，不作为《温病条辨》的后世注家。
2. Wikisource 文本不合格时，允许采用 Kanripo 等许可明确的公开古籍库，并完整记录底本与许可。
3. WorkBuddy 可稳定提供网页搜索和网页读取能力；实施期只需适配具体工具接口。
4. 前端只显示简体，不提供繁体展开；后台仍保留固定版本的繁体来源快照。
5. 轻量卡片版和彩色Markdown降级版均予实现；完成后由实际用户试用确定默认渲染方式，这属于可用性验证而非范围待定项。

## 14. 粗略工作量

本范围不再只是六部原典，还包括约二十种候选扩展作品、动态现代证据层和老年友好交互。若来源文本质量较好，首个完整可试用版预计约3—5周；若多个注本需要OCR修复或人工区分注文，时间会相应增加。应以语料质量门槛为准，不用错误文本换取表面进度。
