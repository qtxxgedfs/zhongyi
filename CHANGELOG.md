# 更新日志

本项目从 `1.0.0` 起采用 [Semantic Versioning 2.0.0](https://semver.org/lang/zh-CN/)：

- `MAJOR`：不兼容的 API、数据库结构、配置或行为变化；
- `MINOR`：向后兼容的新功能或数据库能力扩展；
- `PATCH`：Bug 修复、性能优化、数据清理、内部重构、文档和细节修正。

## [1.1.0] - 2026-09-20

### Added

- 外置 `query-rules.json`：自然问法清理、常见口语/记忆残片扩展、作品与医家路由及代表段落规则。
- 60题自然问法、10组30轮连续追问测试集，以及独立可用性验收脚本和 Phase 6 报告。
- 检索结果的 `match_tier`、核心引文、完整段落展开标记、上下文导航、路由冲突和澄清元数据。
- `study.py --anchor-id` 显式连续追问入口；不保存隐式会话状态。
- 按段落 ID 精确匹配且可重建的影像校字层 `passage-text-corrections.json`。
- 五部核心第二见证候选、代表篇章试校、王冰本分层试跑和 CText《灵枢》人工参考审计记录。

### Changed

- 检索改为精确短语、关键词与宽松字符三路召回，并按原典、精确度、独立可读性和代表段落重排。
- 指定作品或医家没有同等强度结果时执行全库软回退，并明确提示可能记混来源。
- 短残片和弱相关候选默认不展示；长段默认只显示数据库原文的连续核心引文。
- 研究包升级至 schema v2，只保留一个标准研读深度，要求一句话主旨、逐句白话、关键词和有直接依据时的医家异同。
- Markdown/HTML渲染改为核心引文优先；HTML可展开完整段落，无直接依据的B/C层允许省略。
- 《温病条辨》两个 HT 缺字经扫描影像确认修为“祗”“伸”，保持 `WBTB-000004`、`WBTB-000209` 不变并解除隔离；当前53,800条可引用、288条隔离。
- 自动测试增至48项；原150题280项验收保持全通过，自然问法与连续追问验收全部通过。
- 扩充敏感文件、证书、数据库备份和临时产物的 Git 忽略规则；`memo.md` 继续只保留在本地。

### Data and release status

- 26种作品、54,088条结构化段落；53,800条可引用、288条隔离，六部核心当前无隔离段。
- 经典运行时数据库 SHA-256：`35316630ad2105cf9384376062f1c524994b63b633a75fbdd8b41130dbf21fc2`。
- 发布质量闸门仍为 `blocked`：五部核心尚未完成全量第二见证校勘，王冰本仍有805页低校对等级。
- Kanripo 仅作不随包分发的候选比较见证；仓库级许可覆盖、电子转录独立性和影像链仍待确认。

### Compatibility

- 未重切经典语料，已有段落ID保持不变；CLI既有参数继续可用。
- 发布状态仍为 `candidate-runnable`，Phase 1 来源质量闸门仍为 `blocked`。

## [1.0.0] - 2026-09-20

首个维护基线版本。此版本号表示主功能和代码交付进入稳定维护阶段，不表示古籍底本已经通过最终无条件发布质量闸门；运行时状态仍为 `candidate-runnable`，来源发布闸门仍为 `blocked`。

### 新增

- 六部原典及二十种历代医家/学术源流作品的固定修订语料流水线。
- 原始快照、字符映射、繁简转换、结构切分、问题段隔离、质量报告和来源追溯。
- SQLite FTS5 经典检索，支持作品/体系/作者路由、上下文、目录、简繁与术语别名。
- M0—M3 现代医学分级、急症优先、可信来源登记、证据缓存和去身份化联网计划。
- `study.py` 单调用 A/B/C 研究包。
- 可访问 Markdown 与自包含 HTML 渲染。
- 运行时哈希、SQLite 完整性、研究包、渲染和 M3 分流自检。
- 150 题固定功能验收集、索引 benchmark、候选包构建及解压安装测试。
- 安装文档和面向普通读者的用户使用手册。

### 数据状态

- 26 种作品，973 个固定页面快照，938 页进入语料。
- 54,088 条结构化段落；53,798 条可引用，290 条隔离。
- 经典运行时数据库 SHA-256：`76aeadef973a7ac8a0ed4086d975ab893ccd26decf8f3af38663236312c891d8`。
- 现代证据种子缓存：25 个主题、11 个来源机构、6 条完整页面核验记录。

### 验证

- Python 编译检查通过。
- 38/38 项 `unittest` 通过。
- 150 题、280/280 项功能检查通过。
- 运行时自检及候选 ZIP 解压自检通过。

### 已知限制

- 五种核心底本尚未完成第二许可明确底本校核。
- 《温病条辨》仍有 2 个核心隔离语义单元。
- 王冰本包含 805 个上游低校对/问题页面。
- 现代证据缓存目前仅为小规模种子缓存；要求最新资料或缓存不足时需要外部网页能力。
- 当前没有数据库增量迁移框架；schema 或语料变化采用完整重建和整套回滚。
